# cat=basic/1; type=string; label=Directory of the Markdown files, relative to the docroot
outputDirectory = fileadmin/blog_posts

# cat=basic/2; type=int; label=Page UID of the sysfolder into which "fastblog:import" writes the BlogPost records
blogStoragePid = 0
